package com.mimacom.ddd.dm.dem.ui.tests

import org.junit.jupiter.api.Test

class DummyTest {
	@Test def void deleteMe() {
		// only prevent the following error:
		// > ERROR] Failed to execute goal org.eclipse.tycho:tycho-surefire-plugin:1.5.1:tes
		// > (default-test) on project com.mimacom.ddd.dm.dmx.ui.tests: No tests found. -> [Help 1]
	}
}
